import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#1750AC" },
    secondary: { main: "#FFFFFF" },
    background: { default: "#1750AC", paper: "#87CEFA" },
  },
});

export default theme;
